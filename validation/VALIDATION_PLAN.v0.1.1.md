# Validation Plan v0.1.1

Run the complete pytest suite, Alice schema/example validator, simulator automatic/manual capture, failure injection, idempotency, ledger recovery, UI smoke, JSON/JSONL/YAML/Python checks, required-path and version checks, manifest hash verification, native Debian build/install smoke, firmware toolchain check, full ZIP membership validation, and forbidden detached-package scan. Preserve Windows and physical hardware as blocked/not-run without weakening gates.
