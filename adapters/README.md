# Adapters

All network, Git, cloud, MCP, VPort, socket, HTTP, and message-bus adapters are disabled by default. Spreadsheet derivatives never replace JSONL authority and remote failure never blocks the weighing loop.
