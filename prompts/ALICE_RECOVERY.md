# Alice Recovery Prompt

Read `records.jsonl`, verify the hash chain, rebuild `session_snapshot.json`, inspect pending derivative receipts, and present the operator with the last committed sequence. Never infer a successful record from UI state alone.
