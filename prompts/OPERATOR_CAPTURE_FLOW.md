# Operator Capture Flow

Scan barcode → verify active cultivar/container → wait for stable weight → automatic commit or Confirm & Continue → local commit receipt → success feedback → next barcode. Warning and error sounds are distinct from success.
