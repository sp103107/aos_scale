# Human chat kickoff — BBWS-CALUX

Implement the Operator UX ARC in `C:\best_buds_cultivator_weight_station_v0_1_9`.

Rules:

1. Cite `M:\SALVAGE\...` and `M:\OTHER\...` read-only; never modify M: without permission.
2. Write context under `context/operator_ux_arc/`.
3. Fail-closed only for breaking function (no device, bad cal test, mid-capture).
4. Soft-suggest uncalibrated / optional barcode; never block new run for missing calibration.
5. Keep honest non-claims in UI and receipts.
