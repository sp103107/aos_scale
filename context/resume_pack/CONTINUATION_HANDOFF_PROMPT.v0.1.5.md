# Continuation Handoff — Best Buds Weight Station v0.1.5

Begin from the complete `best_buds_cultivator_weight_station_v0_1_5` repository.

First run:

```bash
python -m best_buds_weight_station.bootstrap --profile prehardware
```

Do not begin physical integration unless the prehardware bootstrap passes. Then execute hardware inventory, wiring truth, firmware compile/upload, serial protocol, zero/tare, calibration, repeatability, physical barcode loop, and restart/resume evidence. Preserve all v0.1.5 receipts and non-claims.
