# Continuation Handoff — Best Buds Weight Station v0.1.6

Begin from the complete `best_buds_cultivator_weight_station_v0_1_6` repository.

First run:

```bash
python -m best_buds_weight_station.bootstrap --profile operator-ready
```

The next action is the final naming and drift concordance pass. Verify current names, version references, launcher names, package metadata, generated manifests, historical evidence boundaries, and Cursor handoff paths. Do not rewrite immutable historical episodes, ledger events, or prior receipts.

After concordance, proceed to UNO R3/HX711 physical integration only from a complete full-repository state. Preserve the non-claims for native Windows execution, firmware, and physical hardware until actual evidence exists.
