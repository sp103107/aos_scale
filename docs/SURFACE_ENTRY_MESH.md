# Surface Entry Mesh - v0.1.8

The current machine-readable mesh is `entrypoints/surface_entry_map.v0.1.8.json` and `registry/surface_entry_registry.v0.1.8.json`.

Local software entry points may execute under evidence capture. MCP, message bus, VPort, socket, physical hardware, and standalone pod surfaces remain disabled or candidate-only unless direct activation receipts exist.
