# Alice Operator Guidance

The capture screen displays Alice's current instruction, truth class, required action, blocked actions, and an expandable operator-safe evidence summary. Engineering traces, secrets, tokens, and raw filesystem internals are not shown in normal operator mode. Critical states use text labels in addition to visual styling and remain keyboard accessible.
