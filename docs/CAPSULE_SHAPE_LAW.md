# Capsule Shape Law

This application remains a complete repo-shaped capsule/pod. Historical capsule terminology is preserved while `pods/` is the forward-facing package surface. Pod records are structural declarations only; no pod runtime activation is claimed.
