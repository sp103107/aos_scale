# Recovery and Backup

Verify `records.jsonl`, validate the hash chain, reconstruct the session snapshot, rebuild CSV/XLSX/reports, and inspect pending derivative receipts. Never infer a committed event from a beep, screen, or spreadsheet row alone.
