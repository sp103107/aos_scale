# Architecture

`law → kernel → contracts → prompts → firmware/device adapter → backend state machine/storage → PySide6 frontend → spreadsheet/report adapters → validation → packaging → context`. Platform-specific details remain behind adapters.
